RSpec.describe Graphql::Sorbet::Client do
  it "has a version number" do
    expect(Graphql::Sorbet::Client::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
